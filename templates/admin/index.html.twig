{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block crud %}
<h1 class="offset-2"> <code> <i class="fas fa-user-cog"> </i>  Admin page</code></h1>
    

<div class="container">
        <div class="section">
            <div class="row">
                <div class="col m6 offset m3">
                    <ul>
        
            </ul>
            <table class="table table-striped table-inverse table-responsive center offset-2">
                <thead class="thead-inverse">
                    <tr>
                        <th>ID</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th class="w-25">Manage users</th>
                     
                    </tr>
                    </thead>
                    <tbody>
                            {% for user in users %}
                        <tr>
                            
                            <td> {{user.id}}</td>
                            <td> {{user.email}}</td>
                            <td> {{user.roles|join(',')}}</td>
                            <td>
                            <div class="btn-group">
                                    <a class="btn btn-danger" style="color:white" href="{{path('deleteA',{'value': token}) }}">Delete</a>  
                                    <a class="btn btn-success" style="color:white" href="{{path('editA',{'id': user.id})}}">Edit</a> 
                            </div>
                        </td>
                        </tr>
                        {% endfor %} 
                    </tbody>
            </table>
              <br>
              <br>
              <br>
              
              
             
        
           

                </div>
            </div>
        </div>
    </div>
</div>



{% endblock %}

{% block body1 %}{% endblock %}
